package main

import (
	"fmt"
)

type bytecounter string

func (c *bytecounter) Write(p []byte) {
	*c = bytecounter(p)
}
func main() {
	var c bytecounter
	c.Write([]byte("hello"))
	fmt.Println(c)

	var name = "Dolly"
	fmt.Fprintf(&c, "hello, %s", name)
	fmt.Println(c)
}
